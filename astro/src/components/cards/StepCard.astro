---
interface Props {
  number: string;
  title: string;
  description: string;
}

const { number, title, description } = Astro.props;
---

<div class="step-card">
  <div class="step-number">{number}</div>
  <div class="step-content">
    <h3>{title}</h3>
    <p>{description}</p>
  </div>
</div>
